// Navegación compartida (bottom nav mobile + sidebar). Reusable por todas las pantallas.
// Placeholder — se implementa en el Prompt 4. Por ahora no hace nada (no rompe).

export function mountNav(/* { active } = {} */){
  // TODO (Prompt 4): montar bottom-nav mobile + sidebar desktop, todo girando en torno a "Crear".
}

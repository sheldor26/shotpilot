# secretos — nunca commitear
.env
.env.*

# dependencias / build
node_modules/
venv/
__pycache__/
*.pyc

# salidas de prueba
salida-*.png

# tooling local de agentes (skills instaladas con `npx skills add`)
.agents/
skills-lock.json

# sistema
.DS_Store
